dsh-doc-suite — NOTICE
======================

本模块（work-personal-secretary 的子模块，包名 dsh-doc-suite）
Copyright (c) 2026 liangl1985
Licensed under the MIT License (see LICENSE).

一、随包代码
------------
modules/dsh-doc-suite/ 下的 Node 侧代码（lib/）、Python 脚本（scripts/）、技能（skills/）、
自检脚本（doctor.py）与文档，均为本项目自有代码，采用 MIT 许可。

二、运行时依赖（不随包内置，由使用者安装）
------------------------------------------
本模块的 Python 脚本依赖以下第三方库，其许可与归属如下（请在对外分发时一并复核）：

  python-docx      MIT
  openpyxl         MIT
  python-pptx      MIT
  PyMuPDF          GNU AGPL-3.0（或 Artifex 商业许可）※ 分发前请确认口径
  pdfplumber       MIT
  pypdf            BSD-3-Clause
  Pillow           HPND（MIT-CMU 系）
  pywin32          PSF-2.0

三、外部软件（硬前置，不随包、不内置）
--------------------------------------
  WPS Office —— 使用者自行安装并遵守其许可。本模块仅通过其公开 COM 接口调用
  （比对 / 公式重算 / 数据透视 / 页码与目录域）。

  mermaid 图示运行时（可选能力）—— @mermaid-js/mermaid-cli 与 puppeteer，仅在使用
  「mermaid 图示」时按需安装到使用者数据目录（~/.dsh/data/dsh-doc-suite/tools/mermaid），
  **不写入本模块 dependencies、不进发布件**（避免其 postinstall 下载 Chromium 影响插件安装）。
  由使用者运行 scripts/media/setup_mermaid.ps1 安装或迁移；许可随各上游项目。

四、数据与隐私
--------------
默认**全程在本机完成**：不联网、不上传文档内容、无遥测。

唯一的云端出口是**生图（ARK）**，且需同时满足两个条件才会发生：
  1) 使用者显式调用 gen_image.py 的 image 子命令（设置项 mediaImageEnabled 默认虽为 true，
     它是「渲染图形元素时优先尝试生图」的开关，仍须由调用方显式发起）；
  2) 使用者已配置 ARK 密钥（mediaArkApiKey 或环境变量 ARK_API_KEY，**默认值为空**）。
未配置密钥时生图直接返回「不可用」并自动回退代码矢量绘制 —— **不会发生任何网络请求**。
调用前工具会打印显式告知；使用者须确保 prompt 不含客户信息、报价或涉密内容。
密钥本身不打印、不落日志、不进报错文本，也不随发布件分发。

五、随包素材（母版模板）
------------------------
assets/templates/{dusk,azure,crimson}.pptx —— 三套 PPT 母版（每套 1 个母版 + 11 个版式）：

  来源     WPS 主题库模板 → 抽取风格母版后**清除公司内容**：公司基底母版、业务页、
           业务图片、批注作者与文档元数据均已删除（逐份核查，公司痕迹 0 命中）
  用途     制作 PPT 时的起手母版（`ppt_tool.py create --template <母版>`）
  许可     **授权口径（项目所有者 2026-09-18 明确）：标准仅供参考学习使用；
           如用于商业化，一切后果由使用者自行承担。**
           三套母版来源为 WPS 主题库素材，随包前已清除全部公司内容（公司基底母版、
           业务页、业务图片、批注作者与文档元数据），逐份核查公司痕迹 0 命中。
           使用与再分发的风险由使用者自行判断并承担；`assets/manifest.json` 的
           `redistributable` 按上述口径标记为 `true`（用途限定以本节为准）。

  assets/icons/*.png（6 个图标）为本项目用 Pillow 自绘，MIT，可随包分发。
