# templates/（母版与主题骨架）

**当前状态：目录预留，未含母版文件**（依据 55 号：主题制作 / 母版导入后置）。

## 用途（后置实现）

- templates/<theme>/skeleton.pptx：可选的母版骨架，**只用于「导入公司母版时提取主题 / 版式」**；
  渲染主线是**代码几何** —— specs/*.json 的 pptx 段是唯一真值，不依赖母版。
- 使用者自定义层：~/.dsh/data/dsh-doc-suite/templates/（主题规格 json 与公司母版 pptx 放这里，
  同名文件覆盖内置层；目录级合并规则见 55 号第六章）。

## 发布纪律

**内置层必须中性**：公司口径、母版与素材一律放自定义层，发布件不带。
