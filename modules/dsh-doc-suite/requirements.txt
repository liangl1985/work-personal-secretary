# dsh-doc-suite —— 文档能力模块（Word / Excel / PPT / PDF）
#
# 【硬前置】Python >= 3.10（由 PyMuPDF 1.28+ 的 requires_python 决定；Pillow 12+ 同样要求 >=3.10）
#   本机验证环境：Python 3.12.10（Windows 用 `py -3` 调用；`python` 可能是 MS Store 别名 stub）
# 【硬前置】WPS Office（比对 / 公式重算 / 透视 / 页码目录 全依赖它的 COM 接口）
#
# 安装（在模块目录下执行）：
#   py -3 -m pip install -r requirements.txt          # 必需
# 自检：
#   py -3 doctor.py

# ---- 必需：四格式处理的最小集 ----
python-docx>=1.2.0        # Word 读写（>=3.9）
openpyxl>=3.1.5           # Excel 读写（>=3.8）
python-pptx>=1.0.2        # PPT 制作（>=3.8）
PyMuPDF>=1.28.0           # PDF 只读精确提取（>=3.10 ← 版本下限来源）
pywin32>=310              # WPS COM 调用（Windows；>=3.9）
pdfplumber>=0.11.0        # 表格提取（与 PyMuPDF 互补，>=3.8）
pypdf>=6.0.0              # PDF 结构操作（>=3.9）
Pillow>=10.0.0            # 图片处理（页面转图 / 内嵌图导出）
