# CHANGELOG — work-personal-secretary（工作秘书集成体）

> 本文件记录**集成体整体**的版本历史。集成体自身的语义化版本与子模块版本**解耦**：
> 集成体打 `v<major>.<minor>.<patch>`（例 `v1.0.0`），子模块另可打 `dsh-work-memory@1.0.5` 形式的 tag。
> 各子模块自己的变更史见其目录下的 `CHANGELOG.md`。

## v1.1.1 – v1.1.3 — 2026-09-16 / 09-17（设置界面与目录模型线）· ⛔ **均不发布**

> **发布状态（2026-09-17 主人裁定）**：`v1.1.1` / `v1.1.2` / `v1.1.3` **均不发布** —— 目录模型本次只定义为测试，本机不采用新布局、工作区整体重建另择时间做。细节见 `modules/work-personal-secretary/CHANGELOG.md` 的 1.1.3 段。
> **对外发布版仍为 `v1.0.0`（Latest）**；`v1.1.0` 的 GitHub Release 草稿已于 2026-09-17 删除（tag `v1.1.0` 保留作历史留痕，正文可从本文件恢复）。
> **后续发布节奏（2026-09-17 主人定）**：此后一律**小步 patch 修改，不再开大版本**；非必要不打 Release。
>
> 三版要点（细节见本体 CHANGELOG）：
> - **1.1.1**：核心配置页与「保存即执行」六步链、记忆库迁移步骤、目录模型收窄
> - **1.1.2**：随包说明网页（安装引导 / 使用说明）、文档能力页接入生图设置（密钥链路打通）、桌面外壳 exact 路由接线
> - **1.1.3**：设置界面四页签改版、「一个存储根目录 + `memory-data` / `obsidian-data`」目录模型、旧知识库导入引导卡
>
> **CI 修复（2026-09-17）**：远端 `main` 追平本地 1.1.3 后，修复两处只在 Linux runner 暴露的测试问题 —— `dsh-doc-suite` 的 PIL / python-pptx 守卫与随包 lock 入库（0.7.10），以及本体 `basedeck-test` 的 Windows 专属断言平台适配。
## v1.1.0 — 2026-09-15（每轮注入上限 4 · 通用型与域专家分开评分 · 文档工具编号层级修复 · 桌宠独立化）

- **目录与包名**：`modules/dsh-token-pet` → `modules/workspace-tokenpet`（`git mv`，git 历史保留）；包名 `dsh-token-pet` → `workspace-tokenpet`；版本 `0.2.1-lina.1` → `1.0.0`；**插件运行时 id 一并改为 `workspace-tokenpet`**（cordis entry id/name、客户端 bundle 装载 id、插槽与设置分区 id、路由前缀 `/workspace-tokenpet/...`）。
- **去掉定制层 / 补丁形态**：删除 `patches/**`、`scripts/apply-customizations.*`、`scripts/upstream/**`；README 重写为独立项目文档（定位 / 功能清单 / 安装 / 开发与构建 / 致谢）并新增 `README.en.md`。
- **法律**：模块 `LICENSE` 保留上游 MIT 版权行并新增本项目版权行；`NOTICE` 改写为「本项目 + 致谢上游（项目名 / 仓库 / 许可 / 基线 commit）」。
- **运行时数据目录**：新址 `~/.dsh/data/workspace-tokenpet/skins/`（**新址优先**）；新址缺套装而旧址 `~/.dsh/data/dsh-token-pet/skins/` 有同名套装时**复制迁移**（旧数据保留、绝不覆盖使用者已有套装、单套失败不影响安装结果）。
- **下游同步**：集成体本体（安装器 / 配置底座 / 探针 / 模块清单 / 客户端安装顺序与设置页跳转）与全部测试夹具一并更名。
- **性质口径**：安装页与文档新增「**独立项目模块**」性质（`workspace-tokenpet` 由「第三方」改标；`dsh-mermaid` 仍为「第三方」），上游版权、MIT 许可与致谢表述不变。
- **构建完整性（模块升 1.0.1）**：补入两个从未入库的构建输入（`src/client/*.generated.ts`，+22.5 MB）；`client/client.js` 换为**真实构建产物**（与旧版仅差 1 处注释）；`lib/**` 与 `tsc` 产物 **26/26 逐字节一致**；新增发布门禁「`npm run build` 后 `git diff --exit-code -- lib client` 必须为空」。
- **本机迁移已执行**（主人 2026-09-14）：profile 换 id 至 `workspace-tokenpet`、旧 `dsh-token-pet` 目录清除、素材新址 42 文件在用、旧址保留。

### 专家库 0.1.4（dsh-experts）

- **匹配排序修复**：`rankExperts` 排序键改为「显式指定 > 任务实证（evidence）> 总分 > index 顺序」，`expertMinScore` 只卡**零实证**候选 —— 修掉"跨域单关键词任务被岗位域先验压过"（实测「这份采购合同的钱怎么算、税怎么处理」原先返回投标策略师，现命中法务；上限 2 且无身份专家时「法务 + 会计」可同时选中）。
- **提示词注入分级（L0 / L1 / L2）**：默认只注入**精简卡**（角色首句 + 工作方法前 3 条 + 交付与自检前 2 条 + 适用；实测 **464–509 字符/位**）；新增设置 `expertInjectDetail`（auto / card / **full 可一键回退旧行为**）与 `expertInjectBudgetChars`（默认 1400，超预算按序降级并标注，绝不静默超限）；`expert_recall` 与派子代理内联仍取全文。实测单轮专家注入 3674 → **1218 字符**（降 67%），短任务 1951 → **660 字符**。
- **本体同步**：能力配置页新增两键（形态下拉 + 预算滑块）、`EXPERTS_CONFIG_FALLBACK` 补齐、`settings-api-test.mjs` 对齐 12 键。
- **门禁**：experts 回归 27/27 · 注入分级 16/16 · 装载冒烟 18/18 · 共存 7/7；本体五套 settings-api 109 · install 200 · probe 136 · basedeck 179 · smoke 337，全部 0 失败；`node --check` 全量 57 文件 0 失败。

### 本体自测契约对齐（2026-09-15 · dsh-experts 0.3.x）

CI「集成体本体自测」在 `settings-api-test.mjs` 停红（103 / **6 失败**）：6 条断言仍按 0.2.x 契约写（旧 6 域 id、已删的 `expertMinScore`、身份专家恒选、schema 12 键）。**只改测试夹具与断言、不动产品逻辑**，对齐 0.3.2 口径后 **112 / 0**；本体五套 + experts 四套 + work-memory 回归全绿。本体侧 `EXPERTS_CONFIG_FALLBACK` 与能力配置页的 0.2.x 口径**不在本次范围**，另挂待办。详见 `modules/work-personal-secretary/CHANGELOG.md` 的「契约对齐（2026-09-15）」小节。

### 专家库 0.4.0（dsh-experts）— 注入上限 4 · 通用型与域专家分开评分

- **注入上限 3 → 4**（`INJECT_MAX_HARD`）。实测 20 条真实任务：**0 位 3 · 1 位 14（70%）· 2 位 2 · 3 位 1 · 4 位 0** —— 无一次命中 4 位，故 4 是「留余量」而非「常态化占满」。
- **修复一处静默缺陷**：`selectExperts` 内还硬编码着 `Math.min(3, …)`，此前**无论设置填多少，上限恒为 3**（端到端无效）。教训：只验证函数层（`clampInjectMax(4)===4`）不足以证明链路生效。
- **通用型专家与域专家分开评分**（主人 2026-09-15 定）：域专家＝岗位域先验 0.35 + 相对门槛；通用型专家（核查／排版／文档／演示／设计）＝**恒定通用先验 0.15 + 独立绝对门槛 0.2 + 独立配额 1**。两者**互补而非竞争**（原先通用专家在 `defaultDomain=infosec` 下拿不到岗位先验，结构性吃亏）。
- **门槛默认 0.8 → 0.3**：相对门槛设高会把第 2/3 位挡在外面（0.8 时 70% 任务只命中 1 位；0.3 时 2 位 2→6、3 位 0→2，噪声回归不变）。
- **注入上限不进设置页**（主人定「直接写死」）：默认**写死 4**，设置页不提供该项；如需收紧仍可手改 `settings.yaml` 为 1–3。原滑块（max=3）与 schema 镜像一并移除。
- **配置默认值 2 → 4** 三处一致：`lib/settings.js` DEFAULTS · schema 默认 · `cordis.patch.yml` 的 base（base 会盖过 schema 默认，必须同改）。
- **触发词补缺**：`infosec-ics-security` +`NCS/电厂/发电厂/火电/水电/新能源/电力监控/二次系统/机组/变电站`；`infosec-bid-proposal` +`解决方案/方案设计/方案编写/方案制作/写方案/产品清单/选型`；`finance-research` 收紧过泛词「公司／行业」→「上市公司／行业研究／行业分析」（原先「套公司版式」会误命中投资研究）。
- **端到端效果**：原句「我需要制作一份给电厂的解决方案，电厂需要对 DCS 和 NCS 进行网络安全建设，等级为等保三级…」→ **1 位（等保测评）→ 3 位（工控安全售前 + 等保测评 + 投标与方案策略师）**；22 条真实任务触发分布 **0位2 / 1位11 / 2位6 / 3位2 / 4位1**。
- **`general-typeset` 重定位**（0.3.2 → 0.3.3，提交 `7886c03`）：由「公文手册」改为「成文判断 + 中文文字表述」（删公文细则与重复工具段约 1400 字，补术语词表／去 AI 味／数字标点国标／常见病句／审阅方法／改稿三档）。

### 文档工具 0.2.0（dsh-doc-suite）— 编号层级与 Markdown 解析修复

- `apply-style` **编号层级按深度映射**（`1`→h1 · `1.1`→h2 · `1.2.1`→**h3** · `1.2.3.4`→h4 · 五级→**h5**）；加**标题性判据**（≤40 字、无句末标点、无句子级逗号）与**编号后须有分隔符**（排除 `2020.08.21` 类日期行）；新增**文档级编号体系判定**（公文体 vs 数字点体系）；「编号 vs 命名样式」限定为**只在相差 1 级时纠正**；新增开关 `roles.level_fix`。
- `create` 的 Markdown 解析支持 **1–6 级标题**与 `**加粗**`（原先只认 1–3 级、星号直接写进文档）；规格新增 **Heading 5**（五号 10.5pt；`standard.json` v1.2→v1.3）。
- 回归 19 → **21/0**；端到端：`source.docx` 编号标题判定 10→7、C 组稿 `--dry-run` 错位 46→9 段。

- **注入预算 2000 → 2800（关键，按「4 位最坏消费」定）**：上限写死 4 后，4 位精简卡实测 **当前最坏**（编码 601 + 工控售前 595 + 销售 588 + 设计 583 = 2367）+ banner ≈ **2543**；**理论最坏**（单卡到 card-preview 目标上限 620）4×620 + banner ≈ **2656**。预算 2000 时注入层按序降级成「只留第一位（729 字符）」，即**「上限 4」在默认预算下形同虚设**；实测 1400/1600/1800/2000/2200 **全部只注入 1 位**，**2400 起注入 4 位**。取 **2800** 以覆盖理论最坏并留余量（2600 只够当前最坏、余量仅 57，不够理论最坏）。**预算只是上限**，命中 1 位时仍只注 1 位 —— 成本随命中数按需增长。
- **本体「能力配置页」契约对齐 0.4.0**（顺带清掉待办一条）：`lib/settings-api.js` 的 `EXPERTS_CONFIG_FALLBACK` 由 `presales / 1400 / 0.8 / expertMinScore` → `infosec / 2600 / 0.3 /（删 expertMinScore）`，并**保留 `expertInjectMax: 4`**（该值会原样交给 `selectExperts`，缺键会让 `match.js` 兜底退化成 1 位，导致降级预览与真实注入不一致）；设置页 `DOMAIN_OPTIONS` 旧 6 域 → 真实 6 域（原先选不到真实域）；集成体 `smoke-load` 夹具由 0.2.x 的 10 键升到 0.4.0 的 18 键。
- **顺带清掉**：`cordis.patch.yml` 的**重复键** `expertSecondThreshold: 0.8`（YAML 后者覆盖前者，会把 0.4.0 的 0.3 顶掉）、`dsh-experts/README.md` 重复陈旧行、中英 locale 里的 `1400 / expertMinScore / 1–3` 文案、`RELEASE-CHECKLIST` 的「硬上限 3 / 16 项」。

### 未并入本版（留 v1.1.1）

- **19 张专家卡的「修正 + 扩大」**（正文密实化 + 触发词扩充建议）：A 组 10 张已完成自检（regression 46/0、card-preview 本组全 OK），B 组 9 张在进行；触发词合并需做**全局撞车比对**后再落盘。
## v1.0.0 — 2026-09-13（正式版第一版）

集成体从"记忆插件"演进为**面向通用工作者的工作秘书集成体**，本版为第一次正式发布。

### 集成体构成

| 子模块 | 版本 | 定位 |
|---|---|---|
| `dsh-work-memory` | 1.0.5 | 执行层长期记忆：三级记忆模型（全局永不遗忘 / 热记忆按 TTL 转冷 / 冷归档被用到即转热）+ 转冷预审 + 会话原生注入 + 右侧边栏面板 + 外部知识库镜像 |
| `dsh-doc-suite` | 0.1.4 | 文档能力：Word（处理 + 比对/红线修订）/ Excel（处理 + 重算 + 透视）/ PPT（制作 + 排版）/ PDF（只读精确提取）；Python 脚本 + DSH 原生技能，宿主半只提供 `/doc-doctor` |
| `dsh-experts` | 0.1.3 | 专家库：20 位专家 / 6 域；常驻注入一位「身份专家」，默认再补入至多一位**按问题归属判断**的对口专家（`expertInjectMax` 默认 2） |
| `dsh-token-pet` | 0.2.1-lina.1 | 桌面宠物形象定制层（**三方插件**的上游 MIT 补丁 + 随仓库提供的**完整可安装副本**，含三套形象素材） |

### 随包默认约定（首装即适用）

1. **语言**：尽量使用简体中文回答与思维（推理痕迹同样）；代码、命令、路径、包名、API/字段名、日志与报错原文、专有名词保留英文，不硬翻。随包提供 `defaults/global-memory.seed.md`（全局记忆种子）与 `defaults/AGENTS.zh-CN.md`（工作区指令模板）。
2. **总控兼读制**：主对话保持轻量（拆解 / 派单 / 核对 / 监督 / 纠正 / 对外沟通），具体工作交专家子代理执行。
3. **敏感行业例外（红线）**：军工 / 商密 / 烟草 / 数据安全类内容由主上下文直接处理，不派子代理。
4. **记忆纪律**：全局永不遗忘；热记忆按 TTL 转冷；冷归档被用到即转热；转冷前先做预审。
5. **专家库使用流程**：常驻只有一位身份专家；每轮先判断问题归属，命中单一 → 原生处理，跨域/需独立作业 → 派子代理并内联 persona，未命中 → 原生处理。
6. **中立与隐私**：发布件不含任何个人化身份、私有称呼、私有路径与记忆数据；本地、明文、不联网、无遥测。
7. **第三方合规**：内置 persona 与角色定义均来自许可允许再分发的来源，逐条记录（`experts/index.json` + `NOTICE`）；无 LICENSE 的一律不收录。

### 发布前中立性修复（本次）

正式发布前逐项核对 `RELEASE-CHECKLIST.md`，清理了以下**发布红线**问题：

- **删除遗留私有配置脚本**（`dsh-doc-suite`）：该文件无任何代码引用（死代码），但文件名与内容含私有助手标识、作者机器绝对路径与私有环境变量前缀；随本版删除。模块内工具脚本、技能与运行时行为零变化。
- **专家库文案与注释中性化**（`dsh-experts`）：清除发布件中残留的私有称呼、私有工作区路径与私有业务背景注释；自测夹具改用通用路径。匹配逻辑与运行时行为零变化。
- **域推断词表中性化**（`dsh-experts`）：会话目录 → 域 的推断词表改用**通用岗位/业务目录名**（售前、投标、财务、法务等），不再绑定任何特定工作区结构。
- **仓库文档私有路径清理**：README 的安装状态表改为通用写法，不再出现作者机器的绝对路径。
- **桌宠补丁脚本兼容性**（`dsh-token-pet`）：`scripts/apply-customizations.ps1` 补 UTF-8 BOM —— 此前在 Windows PowerShell 5.1 下中文提示被按 ANSI 读取导致解析失败。

### 工程门禁（本次核验）

- `dsh-work-memory` 回归 **83/83**（并以 `TZ=UTC` 复跑一致）；`dsh-experts` 回归 **25/25** · 装载冒烟 **18/18** · 共存契约 **7/7**；
- 全量 JS `node --check`、全量 Python `py_compile` 通过；CI 在 Node 22 / 24 双版本通过；
- 源码与已安装副本**逐文件 SHA256 一致**。

### 已知限制

- `dsh-doc-suite` 的两条硬前置**不会自动安装**：**Python ≥ 3.10**（建议 3.12，Windows 用 `py -3`）与 **WPS Office**（COM 通道）；就绪性由 `doctor.py` / `/doc-doctor` 检测。
- 专家库中安全域 6 位与法务 2 位标注 `source.review: pending` —— **未经专业复核**，只作专业参考视角，不得据此出具测评结论或法律意见。
- `dsh-token-pet` 为三方插件定制层：以**补丁**记录改造，并随仓库提供**完整可安装副本**（含三套形象素材）；运行时素材位于 `~/.dsh/data/dsh-token-pet/skins/`。（**2026-09-13 修订**：原文为「形态为补丁，需先取上游基线再应用；形象素材属私有资产，不随包分发」，与仓库实际形态不符。）
- 思维链/流程可视化能力仍在候选评估阶段，未随本版提供。
